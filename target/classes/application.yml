spring:
  datasource:
    username: root
    password: root
    url: jdbc:mysql://localhost:3306/habitTrackerDB
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        format_sql: true
  mail:
    host: smtp.gmail.com
    port: 587
    username: "rubanrio2003@gmail.com"
    password: "qtxa pasg ejph tlvo"
    properties:
      mail:
        smtp:
          auth: true
          starttls:
            enable: true

base:
  url: "http://localhost:8080"

logging:
  level:
    org.springframework.security: DEBUG
